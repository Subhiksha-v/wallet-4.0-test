{
  "name": "p2p-app-root",
  "version": "1.0.0",
  "description": "Root package for P2P App (Frontend & Backend)",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start --prefix ./backend\" \"npm run dev --prefix ./frontend\"",
    "install-all": "npm install && npm install --prefix ./backend && npm install --prefix ./frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
